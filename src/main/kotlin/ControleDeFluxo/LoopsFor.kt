fun main() {

    val cakes = listOf("carrot", "cheese", "chocolate") // uma lista de bolos, utilizando o listOf

    for (cake in cakes) { // para cada elemento na lista cakes, atribua o valor a variavel cake.
        println("Yummy, it's a $cake cake!") // imprimindo o resultado concatenando com a expressao.
    }

}